apiVersion: v1
kind: Service
metadata:
  name: fiapfood-producao-service
spec:
  selector:
    app: fiapfood-api-producao
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081
    #nodePort: 30003
  type: LoadBalancer
